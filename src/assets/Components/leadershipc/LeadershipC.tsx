
{/*

type LeaderProps = {
  name: string;
  role: string;
  image: string;
  bio: string;
  social: {
    linkedin?: string;
    twitter?: string;
    facebook?: string;
    instagram?: string;
  };
};

const LeaderCard: React.FC<LeaderProps> = ({ name, role, image, bio, social }) => {
  return (
    <div className="leaderCard">
      <img src={image} alt={name} className="leaderImage" />
      <h2>{name}</h2>
      <p className="leaderRole">{role}</p>
      <p className="leaderBio">{bio}</p>
      <div className="leaderSocial">
        {social.linkedin && <a href={social.linkedin}>LinkedIn</a>}
        {social.twitter && <a href={social.twitter}>Twitter</a>}
        {social.facebook && <a href={social.facebook}>Facebook</a>}
        {social.instagram && <a href={social.instagram}>Instagram</a>}
      </div>
    </div>
  );
};

export default LeaderCard;
*/}